{
  "name": "@indestructibleeco/shared-types",
  "version": "0.1.0",
  "description": "Shared TypeScript type definitions — zero runtime cost",
  "types": "src/index.ts",
  "scripts": { "typecheck": "tsc --noEmit" },
  "devDependencies": { "typescript": "^5.5.0" }
}